define class Circle:
    define __init__(self, radius):
        self.radius = radius;
    define area(self):
        return self.radius ** 2 * 3.14;
    define perimeter(self):
        return 2 * 3.14 * self.radius;
c1 = Circle(87);
print(c1.area());
print(c1.perimeter());
